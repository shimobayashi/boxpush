# BOXPUSH

ブラウザで遊ぶ箱押しパズル。全 30 面、固定。GitHub Pages で公開する。

仕様と、そう決めた理由は `docs/design.md` にある。作る前に読む。

## 用語

**このジャンルは「箱押しパズル」と呼ぶ。**

このジャンルには有名な商用ゲームのタイトルがあり、それは他社の登録商標になっている。
ジャンル名のつもりで使っても、無関係な個人が公開するゲームで使えば、
その作品の名前を借りているように見える。呼び方を変えても中身は何も変わらないので、避ける方を採る。

そのタイトルは日本語でも英語でも書かない。この注意書きの中でも書かない。
英語では `boxpush` / `push puzzle` などにする。

コード・コメント・コミットメッセージ・PR 本文・画面の文言・README、すべてに当てはまる。

盤面の記号（`#` `@` `$` `.` `*` `+`）は、このジャンルで広く使われている書式なのでそのまま使う。

## このリポジトリは Public

外に出て困る情報を置かない。認証情報、個人情報、外部から持ち込んだ素材を、
ファイルにもコミットメッセージにも残さない。

音は Web Audio で合成し、絵は図形で描く。素材ファイルを持たないので、ライセンスの確認も要らない。

## 面を足すとき

面は `levels.txt` に記号で書く。書式は `docs/design.md` の「面データ」。

面は生成スクリプトが目標の難易度に合わせて選ぶ。手で選ばない。
足すときも、テストで「解けること」「難易度が表のとおり並んでいること」を確かめてから入れる。
